# ============================================
# Arquivo de Variáveis de Ambiente - PRODUÇÃO
# ============================================
# 
# IMPORTANTE: Este arquivo contém variáveis sensíveis.
# NUNCA commite este arquivo com valores reais no Git!
# Use este arquivo como template e configure os valores reais no servidor.
#
# Para usar este arquivo:
# 1. Copie este arquivo para .env.production no servidor
# 2. Preencha todos os valores com dados reais
# 3. Certifique-se de que o arquivo está no .gitignore
# ============================================

# ============================================
# Configurações do Servidor
# ============================================
# Porta em que a aplicação Next.js irá rodar
PORT=3000

# ============================================
# Better Auth - Autenticação
# ============================================
# Secret usado para criptografar tokens e sessões
# DEVE ter pelo menos 32 caracteres
# Gere um valor seguro usando: openssl rand -base64 32
BETTER_AUTH_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARACTERS_LONG

# URL base do Better Auth (opcional)
# Use apenas se a API de autenticação estiver em um domínio diferente
# Exemplo: https://api.seudominio.com/api/auth
BETTER_AUTH_URL=

# URL pública do Better Auth para o cliente (opcional)
# Use apenas se necessário para configuração do cliente
NEXT_PUBLIC_BETTER_AUTH_URL=

# ============================================
# CORS - Cross-Origin Resource Sharing
# ============================================
# Origem permitida para requisições CORS
# Configure com o domínio da sua aplicação em produção
# Exemplo: https://app.seudominio.com
CORS_ORIGIN=https://app.seudominio.com

# ============================================
# Banco de Dados Principal (PostgreSQL)
# ============================================
# URL de conexão com o banco de dados PostgreSQL principal
# Formato: postgresql://usuario:senha@host:porta/database
# Exemplo: postgresql://postgres:senha123@localhost:5432/gestor_db
DATABASE_URL=postgresql://usuario:senha@host:5432/database

# ============================================
# Envio de Email
# ============================================
# O sistema suporta dois provedores: Resend (recomendado para produção)
# ou SMTP/Gmail (para desenvolvimento/testes)

# --- Opção 1: Resend (Produção) ---
# API Key do Resend para envio de emails
# Obtenha em: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxx

# Email remetente padrão
# Formato: "Nome <email@dominio.com>"
# IMPORTANTE: O domínio deve estar verificado no Resend
EMAIL_FROM="FBI Gestor <noreply@seudominio.com>"

# --- Opção 2: Gmail/SMTP (Desenvolvimento) ---
# Para usar Gmail, você precisa:
# 1. Ativar "Verificação em duas etapas" na sua conta Google
# 2. Criar uma "Senha de app" em: https://myaccount.google.com/apppasswords
# 3. Usar essa senha no SMTP_PASS (não a senha da conta)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx

# ============================================
# Variáveis Adicionais (se necessário)
# ============================================
# Adicione outras variáveis de ambiente específicas da sua aplicação aqui

# ============================================
# Notas de Segurança
# ============================================
# 1. BETTER_AUTH_SECRET: Gere um valor único e seguro para produção
#    - Use: openssl rand -base64 32
#    - Ou: node -e "console.log(require(''crypto'').randomBytes(32).toString(''base64''))"
#
# 2. DATABASE_URL: Use credenciais fortes e diferentes das de desenvolvimento
#
# 3. CORS_ORIGIN: Configure apenas com domínios confiáveis
#
# 4. Nunca compartilhe este arquivo com valores reais
#
# 5. Use um gerenciador de secrets (ex: Docker secrets, Kubernetes secrets, etc.)
#    em ambientes de produção para maior segurança
