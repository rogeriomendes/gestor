generator client {
  provider = "prisma-client"
  output   = "../generated"
  moduleFormat = "esm"
}

datasource db {
  provider = "mysql"
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model adm_modulo {
  ID        Int     @id @default(autoincrement()) @db.UnsignedInt
  CODIGO    String? @db.Char(3)
  NOME      String? @db.VarChar(100)
  DESCRICAO String? @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model adm_parametro {
  ID                                     Int      @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA                             Int      @db.UnsignedInt
  FIN_PARCELA_ABERTO                     Int?     @db.UnsignedInt
  FIN_PARCELA_QUITADO                    Int?     @db.UnsignedInt
  FIN_PARCELA_QUITADO_PARCIAL            Int?     @db.UnsignedInt
  FIN_TIPO_RECEBIMENTO_EDI               Int?     @db.UnsignedInt
  COMPRA_FIN_DOC_ORIGEM                  Int?     @db.UnsignedInt
  COMPRA_CONTA_CAIXA                     Int?     @db.UnsignedInt
  ID_CLIENTE_PADRAO                      Int      @db.UnsignedInt
  ST_NOVO_CLIENTE_BLOQUEADO              String   @default("N") @db.VarChar(1)
  ID_SITUACAO_CLI_PADRAO                 Int
  ID_SITUACAO_CLI_BLOQUEADO              Int
  ID_SITUACAO_FOR_PADRAO                 Int
  ID_ATIVIDADE_CLI_PADRAO                Int?
  ID_ATIVIDADE_FOR_PADRAO                Int?
  ID_FIN_DOCUMENTO_ORIGEM_RECEBER        Int?     @db.UnsignedInt
  ID_CENTRO_RESULTADO_RECEBER            Int?     @db.UnsignedInt
  ID_CONTA_CAIXA_RECEBER                 Int?     @db.UnsignedInt
  ID_CENTRO_RESULTADO_PAGAR              Int?
  ID_NATUREZA_FINANCEIRA_PAGAR           Int?
  ID_NATUREZA_FINANCEIRA_RECEBER         Int?
  ID_TABELA_PRECO                        Int?
  ID_NATUREZA_FINANCEIRA_DEVOLUCAO       Int?     @db.UnsignedInt
  FIN_PARCELA_RENEGOCIADA                Int?     @db.UnsignedInt
  TIPO_IMPRESSAO_DAV                     Int?     @default(0) @db.UnsignedInt
  TIPO_AVISO_ESTOQUE                     Int?     @default(0) @db.UnsignedInt
  IMPRIMIR_ID_VENDA_DAV                  String?  @default("N") @db.Char(1)
  DAV_NUMERO_COPIAS                      Int?     @default(1) @db.UnsignedInt
  DAV_IMPRIMIR_ASSINATURA                String?  @default("N") @db.Char(1)
  CPF_CNPJ_REPEAT_CLIENTE                String?  @default("N") @db.Char(1)
  CPF_CNPJ_REPEAT_FORNECEDOR             String?  @default("N") @db.Char(1)
  EMITE_NFC                              String?  @default("N") @db.Char(1)
  VALORESDIVERGENTES                     String?  @default("S") @db.Char(1)
  TOTALIZADOR_PESO                       String?  @default("N") @db.Char(1)
  USAR_LEITOR_CODIGO_BARRAS              String?  @default("N") @db.Char(1)
  PADRAO_ENTREGA                         String?  @default("N") @db.Char(1)
  IMPRIMI_DAV_ENTREGA                    String?  @default("N") @db.Char(1)
  GERAR_TITULO_TRANSF                    String?  @default("N") @db.Char(1)
  ID_NATUREZA_FINANCEIRA_SALARIO         Int?
  ID_NATUREZA_FINANCEIRA_TERCEIRO        Int?
  ID_NATUREZA_FINANCEIRA_MATERIAL_GASTO  Int?
  MSG_DAV                                String?  @db.VarChar(300)
  MSG_NFCE                               String?  @db.VarChar(300)
  MSG_NFE                                String?  @db.VarChar(300)
  INTEGRADO_MATRIZ                       String   @default("N") @db.Char(20)
  SELECIONAR_IMPRESSORA                  String?  @default("N") @db.Char(1)
  ID_GRUPO_PADRAO                        Int?
  QUANT_CARACTERES_CODIGO_BALANCA        Int?     @db.UnsignedInt
  INFORMAR_TROCO                         String?  @default("N") @db.Char(1)
  UTILIZA_PAFECF                         String?  @default("N") @db.Char(1)
  MARKET                                 String?  @default("N") @db.Char(1)
  ID_CLIENTE_PADRAO_FUNCAO               Int?
  ID_PLANO_CONTA_FUNCAO_ENTRADA          Int?
  ID_TIPO_RECEBIMENTO_FUNCAO             Int?
  ID_CENTRO_RESULTADO_FUNCAO_ENTRADA     Int?
  ID_FORNECEDOR_PADRAO_FUNCAO            Int?
  ID_PLANO_CONTA_FUNCAO_SAIDA            Int?
  ID_TIPO_PAGAMENTO_FUNCAO               Int?
  ID_CENTRO_RESULTADO_FUNCAO_SAIDA       Int?
  CAMINHO_ARQUIVO_BALANCA                String?  @db.VarChar(100)
  EXIBIR_RANKING                         String?  @default("S") @db.Char(1)
  NFCE_ITEM_UMA_LINHA                    String?  @default("S") @db.Char(1)
  NFCE_RESUMIDO                          String?  @default("N") @db.Char(1)
  NFCE_IMPRE_DESC_ACRE                   String?  @default("N") @db.Char(1)
  FRETE_FIN_DOC_ORIGEM                   Int?
  DAV_RESUMIDO                           String?  @default("N") @db.Char(1)
  BALANCA_MODELO                         Int?     @default(0)
  BALANCA_PORTA                          String?  @default("COM1") @db.VarChar(4)
  BALANCA_BAUD                           Int?     @default(9600)
  BALANCA_DATA                           Int?     @default(8)
  BALANCA_PARITY                         Int?     @default(0)
  BALANCA_HANDSHAKE                      Int?     @default(0)
  BALANCA_STOP                           Int?     @default(0)
  BALANCA_TIMEOUT                        Int?     @default(5000)
  RECIBO_MODELO                          String?  @default("1") @db.Char(1)
  ALTERAR_VALOR_UNITARIO                 String?  @default("S") @db.Char(1)
  ADD_VALE_VALOR_SUPERIOR                String?  @default("N") @db.Char(1)
  CONSULTA_FINANCEIRO_ORCAMENTO          String?  @default("N") @db.Char(1)
  SOLICITA_CLIENTE                       String?  @default("N") @db.Char(1)
  CANHOTO_ENTREGA_DAV                    String?  @default("N") @db.Char(1)
  BLOQUEIA_CLIENTE_SELECAO               String?  @default("N") @db.Char(1)
  BALANCA_ARREDONDA_TRUNCA               String?  @default("T") @db.Char(1)
  CAMINHO_GED_PAGAR                      String?  @db.VarChar(150)
  CAMINHO_EXPORTAR_MOBILE                String?  @db.VarChar(100)
  CAMINHO_IMPORTAR_MOBILE                String?  @db.VarChar(100)
  NFCE_LAYOUT_DANFE                      String?  @default("CUPOM") @db.VarChar(10)
  CHEF_QUANTIDADE_MESA                   Int?     @default(77)
  PERMITE_SERVICO_VENDA                  String?  @default("N") @db.Char(1)
  PERMITIR_CPF_VAZIO                     String?  @default("N") @db.Char(1)
  PERMITIR_CNPJ_VAZIO                    String?  @default("N") @db.Char(1)
  CHEF_IMPRESSORA_COZINHA                String?  @default("GENERICA") @db.VarChar(20)
  TIPO_ETQ_BALANCA                       String?  @default("P") @db.Char(1)
  QT_CARACT_PESO                         Int?     @default(5)
  VENDA_FUNCAO_CAIXA                     String?  @default("N") @db.Char(1)
  VENDA_VISUALIZA_RANKING_VENDEDOR       String?  @default("N") @db.Char(1)
  TIPO_IMPRESSAO_PDV                     Int?     @default(3)
  PDV_FECHAR_CAIXA                       String?  @default("S") @db.Char(1)
  CALCULAR_PESO_ITEM                     String?  @default("N") @db.Char(1)
  EMITE_NFE_PDV                          String?  @default("S") @db.Char(1)
  EMITIR_NFE_PDV_CONTROL                 String?  @default("S") @db.Char(1)
  ENTER_CODIGO_FATURAR                   String?  @default("N") @db.Char(1)
  MOSTRAR_TELA_NR_VIAS                   String?  @default("S") @db.Char(1)
  PDV_ACEITA_CHEQUE                      String?  @default("S") @db.Char(1)
  PDV_ACEITA_CREDIARIO                   String?  @default("S") @db.Char(1)
  ID_PRODUTO_DIVERSOS                    Int?     @default(0)
  PDV_IMPRIMIR_COMPROVANTE_VENDA         String?  @default("S") @db.Char(1)
  PDV_IMPRIMIR_CUPOM_FISCAL              String?  @default("S") @db.Char(1)
  DSN_ALTERAR_VENDA_FATURADA             String?  @default("N") @db.Char(1)
  PDV_MANTER_ULTIMO_COD_PROD             String?  @default("N") @db.Char(1)
  PDV_ALTERAR_EMPRESA_DFE                String?  @default("N") @db.Char(1)
  PDV_ENVIAR_NFCE_PELA_UNFE              String?  @default("N") @db.Char(1)
  USAR_CAD_PRODUTO_SIMPLES               String?  @default("N") @db.Char(1)
  PERMITIR_CLIENTE_SEM_ENDERECO          String?  @default("N") @db.Char(1)
  PREENCHER_CODIGO_INTERNO_PRODUTO       String?  @default("S") @db.Char(1)
  FOOD_MESA_ATIVA                        String?  @default("S") @db.Char(1)
  FOOD_COMANDA_ATIVA                     String?  @default("S") @db.Char(1)
  FOOD_DELIVERY_ATIVA                    String?  @default("S") @db.Char(1)
  FOOD_IMPRIMIR_SENHA                    String?  @default("N") @db.Char(1)
  FAST_FOOD_ATIVO                        String?  @default("N") @db.Char(1)
  FOOD_IMPRIMIR_COZINHA_DIRETO           String?  @default("S") @db.Char(1)
  ENTER_FECHA_VENDA                      String?  @default("N") @db.Char(1)
  PDV_PERMITIR_RECUPERAR                 String?  @default("S") @db.Char(1)
  DESABILITAR_ATUALIZAR_NCM_SEFAZ        String?  @default("S") @db.Char(1)
  ABRIR_CONTA_REC_SEM_FILTRO             String?  @default("N") @db.Char(1)
  ABRIR_CONTA_PAG_SEM_FILTRO             String?  @default("N") @db.Char(1)
  PDV_FECHA_ORC_DIRETO                   String?  @default("N") @db.Char(1)
  VENDA_ABRIR_STATUS_NR_ORC              String?  @default("S") @db.Char(1)
  VENDA_DESABILITAR_ORC                  String?  @default("N") @db.Char(1)
  VENDA_IMPRIMIR_DAV_SEM_ITENS           String?  @default("N") @db.Char(20)
  VERSAO_TABELA_IBPTAX_ATUAL             String?  @default("18.2.A") @db.VarChar(20)
  PDV_AGLUTINAR_ITEM_CUPOM               String?  @default("N") @db.Char(1)
  PRODUTO_SEM_GTIN_NF                    String?  @db.VarChar(20)
  PRODUTO_BUSCA_POR_PARTE                String?  @default("N") @db.Char(1)
  PERMITIR_FORNECEDOR_SEM_ENDERECO       String?  @db.Char(1)
  MINHA_EMPRESA_ATIVA                    String?  @default("S") @db.Char(1)
  MINHA_EMPRESA_MES                      String?  @default("S") @db.Char(1)
  MINHA_EMPRESA_DIA                      String?  @default("S") @db.Char(1)
  MINHA_EMPRESA_PAGAR                    String?  @default("S") @db.Char(1)
  MINHA_EMPRESA_RECEBER                  String?  @default("S") @db.Char(1)
  PDV_VER_USU_LOGIN                      String?  @default("N") @db.Char(1)
  FOOD_ID_SUB_GRUPO_PONTO                Int?
  FOOD_ID_SUB_GRUPO_1                    Int?
  FOOD_ID_SUB_GRUPO_2                    Int?
  FOOD_ID_SUB_GRUPO_3                    Int?
  FOOD_ID_SUB_GRUPO_4                    Int?
  FOOD_ID_SUB_GRUPO_5                    Int?
  FOOD_ID_SUB_GRUPO_6                    Int?
  FOOD_ID_SUB_GRUPO_7                    Int?
  FOOD_ID_SUB_GRUPO_8                    Int?
  TIPO_IMP_OS_REC                        Int?
  CAMINHO_BUSCA_PRECO                    String?  @db.VarChar(250)
  USA_CONDICOES_PRESTACAO_SERVICO_OS     String?  @default("N") @db.Char(1)
  TERMO_CONDICOES_PRESTACAO_SERVICO_OS   String?  @db.VarChar(1000)
  CONFERENCIA_CEGA_CAIXA                 String?  @default("N") @db.Char(1)
  TROCAR_VENDEDOR_VENDA_PDV              String?  @default("N") @db.Char(1)
  USA_CFG_BALANCA_CONTA_CAIXA            String?  @default("N") @db.Char(1)
  PERMITIR_MUDAR_FASE_PARECER_OS         String?  @default("N") @db.Char(1)
  SALVAR_PARECER_SEM_DESCRICAO_OS        String?  @default("N") @db.Char(1)
  SALVAR_OS_SEM_PARECER                  String?  @default("N") @db.Char(1)
  PDV_BUSCA_PRODUTO_TOUCH                String?  @default("N") @db.Char(1)
  SOLICITAR_CONF_IMPR_RELAT_FECHAR_CAIXA String?  @default("N") @db.Char(1)
  IMPR_RELVNDGRUPO_FECH_CX_RES           String?  @default("N") @db.Char(1)
  IMPR_RELVNDGRUPO_FECH_CX_CONFCEGA      String?  @default("N") @db.Char(1)
  IMPR_REL_PEDIDOS_ABERTOS_FECHA_CX      String?  @default("N") @db.Char(1)
  FIN_REC_TAXA_JUROS_DIA                 Decimal? @default(0.000000) @db.Decimal(18, 6)
  FIN_REC_TAXA_MULTA                     Decimal? @default(0.000000) @db.Decimal(18, 6)
  VERIFICAR_INTERNET                     Int?     @default(4)
  USA_MOD_CUPOM_REDUZIDO                 String?  @default("N") @db.Char(1)
  EXIBIR_VALOR_SERVICO_RECIBO_OS         String?  @default("N") @db.Char(1)
  AGRUPAR_ITENS_NA_VENDA                 String?  @default("N") @db.Char(1)
  MOSTRAR_CODIGO_BARRAS_ID_ORCAMENTO     String?  @default("N") @db.Char(1)
  EXIBIR_NAO_EH_DOCUMENTO_FISCAL         String?  @default("N") @db.Char(1)
  PDV_MOSTRA_SUB_GRUPO_TOUCH             String?  @default("N") @db.Char(1)
  EMITE_SAT_CFE                          String?  @default("N") @db.Char(1)
  TIPO_IMPRESSAO_OS_ORC                  Int?     @default(1)
  IMPRIMIR_RELATORIO_ZEBRADO             String?  @default("N") @db.Char(1)
  PDV_OFFLINE_ATIVO                      String?  @default("N") @db.Char(1)
  PDV_PEDIR_CPF_CNPJ                     String?  @default("N") @db.Char(1)
  PDV_ABRIR_CLIENTE_RESUMIDO             String?  @default("N") @db.Char(1)
  PDV_DEVOLUCAO_SEM_CLIENTE              String?  @default("N") @db.Char(1)
  PDV_IMPRIMIR_COMPROVANTE_DEVOLUCAO     String?  @default("N") @db.Char(1)
  COZINHA_NUMERO_COPIAS                  Int?     @default(1) @db.UnsignedInt
  CONFERENCIA_NUMERO_COPIAS              Int?     @default(1) @db.UnsignedInt
  MOSTRAR_TELA_NR_VIAS_REST              String?  @default("S") @db.Char(1)
  PRODUTO_SEM_GTIN_NFC                   String?  @db.VarChar(20)
  empresa                                empresa  @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "adm_parametro_ibfk_1")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_PARAMETRO")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model auditoria {
  ID                Int       @id @default(autoincrement()) @db.UnsignedInt
  ID_USUARIO        Int       @db.UnsignedInt
  DATA_REGISTRO     DateTime? @db.Date
  HORA_REGISTRO     String?   @db.VarChar(8)
  JANELA_CONTROLLER String?   @db.VarChar(50)
  ACAO              String?   @db.VarChar(50)
  CONTEUDO          String?   @db.Text
  usuario           usuario   @relation(fields: [ID_USUARIO], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "auditoria_ibfk_1")

  @@index([ID_USUARIO], map: "FK_USUARIO_AUDITORIA")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cargo {
  ID          Int           @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA  Int           @db.UnsignedInt
  NOME        String?       @db.VarChar(50)
  DESCRICAO   String?       @db.Text
  SALARIO     Decimal?      @db.Decimal(18, 6)
  CBO_1994    String?       @db.VarChar(10)
  CBO_2002    String?       @db.VarChar(10)
  empresa     empresa       @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "cargo_ibfk_1")
  colaborador colaborador[]

  @@index([ID_EMPRESA], map: "FK_EMPRESA_CARGO")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cep {
  ID                    Int     @id @default(autoincrement()) @db.UnsignedInt
  CEP                   String? @db.VarChar(8)
  LOGRADOURO            String? @db.VarChar(60)
  COMPLEMENTO           String? @db.VarChar(60)
  BAIRRO                String? @db.VarChar(60)
  MUNICIPIO             String? @db.VarChar(60)
  UF                    String? @db.Char(2)
  CODIGO_IBGE_MUNICIPIO Int?    @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cnae {
  ID           Int            @id @default(autoincrement()) @db.UnsignedInt
  CODIGO       String?        @db.VarChar(7)
  DENOMINACAO  String?        @db.Text
  empresa_cnae empresa_cnae[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model colaborador {
  ID                       Int                  @id @default(autoincrement()) @db.UnsignedInt
  ID_CONTABIL_CONTA        Int?                 @db.UnsignedInt
  ID_SINDICATO             Int?                 @db.UnsignedInt
  ID_TIPO_ADMISSAO         Int?                 @db.UnsignedInt
  ID_SITUACAO_COLABORADOR  Int                  @db.UnsignedInt
  ID_PESSOA                Int                  @db.UnsignedInt
  ID_TIPO_COLABORADOR      Int                  @db.UnsignedInt
  ID_NIVEL_FORMACAO        Int                  @db.UnsignedInt
  ID_CARGO                 Int                  @db.UnsignedInt
  ID_SETOR                 Int                  @db.UnsignedInt
  MATRICULA                String?              @db.VarChar(10)
  FOTO_34                  String?              @db.Text
  DATA_CADASTRO            DateTime?            @db.Date
  DATA_ADMISSAO            DateTime?            @db.Date
  VENCIMENTO_FERIAS        DateTime?            @db.Date
  DATA_TRANSFERENCIA       DateTime?            @db.Date
  FGTS_OPTANTE             String?              @db.Char(1)
  FGTS_DATA_OPCAO          DateTime?            @db.Date
  FGTS_CONTA               Int?                 @db.UnsignedInt
  PAGAMENTO_FORMA          String?              @db.Char(1)
  PAGAMENTO_BANCO          String?              @db.VarChar(10)
  PAGAMENTO_AGENCIA        String?              @db.VarChar(10)
  PAGAMENTO_AGENCIA_DIGITO String?              @db.Char(1)
  PAGAMENTO_CONTA          String?              @db.VarChar(10)
  PAGAMENTO_CONTA_DIGITO   String?              @db.Char(1)
  EXAME_MEDICO_ULTIMO      DateTime?            @db.Date
  EXAME_MEDICO_VENCIMENTO  DateTime?            @db.Date
  PIS_DATA_CADASTRO        DateTime?            @db.Date
  PIS_NUMERO               String?              @db.VarChar(12)
  PIS_BANCO                String?              @db.VarChar(10)
  PIS_AGENCIA              String?              @db.VarChar(10)
  PIS_AGENCIA_DIGITO       String?              @db.Char(1)
  CTPS_NUMERO              String?              @db.VarChar(10)
  CTPS_SERIE               String?              @db.VarChar(10)
  CTPS_DATA_EXPEDICAO      DateTime?            @db.Date
  CTPS_UF                  String?              @db.Char(2)
  DESCONTO_PLANO_SAUDE     String?              @db.Char(1)
  SAI_NA_RAIS              String?              @db.Char(1)
  CATEGORIA_SEFIP          String?              @db.Char(2)
  OBSERVACAO               String?              @db.Text
  OCORRENCIA_SEFIP         Int?                 @db.UnsignedInt
  CODIGO_ADMISSAO_CAGED    Int?                 @db.UnsignedInt
  CODIGO_DEMISSAO_CAGED    Int?                 @db.UnsignedInt
  CODIGO_DEMISSAO_SEFIP    Int?                 @db.UnsignedInt
  DATA_DEMISSAO            DateTime?            @db.Date
  CODIGO_TURMA_PONTO       String?              @db.Char(5)
  CAGED_APRENDIZ           String?              @db.Char(1)
  CAGED_DEFICIENCIA        String?              @db.Char(1)
  ENTREGADOR               String?              @default("N") @db.Char(1)
  COLABORADOR_PETSHOP      String?              @default("N") @db.Char(1)
  setor                    setor                @relation(fields: [ID_SETOR], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_1")
  cargo                    cargo                @relation(fields: [ID_CARGO], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_2")
  tipo_colaborador         tipo_colaborador     @relation(fields: [ID_TIPO_COLABORADOR], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_3")
  nivel_formacao           nivel_formacao       @relation(fields: [ID_NIVEL_FORMACAO], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_4")
  pessoa                   pessoa               @relation(fields: [ID_PESSOA], references: [ID], onDelete: Cascade, map: "colaborador_ibfk_5")
  situacao_colaborador     situacao_colaborador @relation(fields: [ID_SITUACAO_COLABORADOR], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_6")
  tipo_admissao            tipo_admissao?       @relation(fields: [ID_TIPO_ADMISSAO], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_7")
  sindicato                sindicato?           @relation(fields: [ID_SINDICATO], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_8")
  contabil_conta           contabil_conta?      @relation(fields: [ID_CONTABIL_CONTA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "colaborador_ibfk_9")
  usuario                  usuario[]

  @@index([ID_CARGO], map: "FK_CARGO_COLABORADOR")
  @@index([ID_SETOR], map: "FK_COLABORADOR_SETOR")
  @@index([ID_CONTABIL_CONTA], map: "FK_CTA_CONTAB_COLAB")
  @@index([ID_NIVEL_FORMACAO], map: "FK_NIVEL_FORMACAO_COLABORADOR")
  @@index([ID_PESSOA], map: "FK_PESSOA_COLABORADOR")
  @@index([ID_SINDICATO], map: "FK_SINDICATO_COLAB")
  @@index([ID_SITUACAO_COLABORADOR], map: "FK_SITUACAO_COLABORADOR")
  @@index([ID_TIPO_ADMISSAO], map: "FK_TIPO_ADMISSAO_COLAB")
  @@index([ID_TIPO_COLABORADOR], map: "FK_TIPO_COLABORADOR")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contabil_conta {
  ID                      Int                  @id @default(autoincrement()) @db.UnsignedInt
  ID_CONTABIL_CONTA       Int?                 @db.UnsignedInt
  ID_PLANO_CONTA          Int                  @db.UnsignedInt
  ID_PLANO_CONTA_REF_SPED Int                  @db.UnsignedInt
  CLASSIFICACAO           String?              @db.VarChar(30)
  TIPO                    String?              @db.Char(1)
  DESCRICAO               String?              @db.VarChar(100)
  DATA_INCLUSAO           DateTime?            @db.Date
  SITUACAO                String?              @db.Char(1)
  NATUREZA                String?              @db.Char(1)
  PATRIMONIO_RESULTADO    String?              @db.Char(1)
  LIVRO_CAIXA             String?              @db.Char(1)
  DFC                     String?              @db.Char(1)
  ORDEM                   String?              @db.VarChar(20)
  CODIGO_REDUZIDO         String?              @db.VarChar(10)
  CODIGO_EFD              String?              @db.Char(2)
  colaborador             colaborador[]
  plano_conta_ref_sped    plano_conta_ref_sped @relation(fields: [ID_PLANO_CONTA_REF_SPED], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "contabil_conta_ibfk_1")
  plano_conta             plano_conta          @relation(fields: [ID_PLANO_CONTA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "contabil_conta_ibfk_2")
  contabil_conta          contabil_conta?      @relation("contabil_contaTocontabil_conta", fields: [ID_CONTABIL_CONTA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "contabil_conta_ibfk_3")
  other_contabil_conta    contabil_conta[]     @relation("contabil_contaTocontabil_conta")
  sindicato               sindicato[]

  @@index([ID_CONTABIL_CONTA], map: "FK_CONTABIL_CONTA_CONTA")
  @@index([ID_PLANO_CONTA_REF_SPED], map: "FK_CONTA_SPED_CONTA")
  @@index([ID_PLANO_CONTA], map: "FK_PLANO_CONTA_CONTA")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contador {
  ID             Int       @id @default(autoincrement()) @db.UnsignedInt
  ID_PESSOA      Int       @db.UnsignedInt
  INSCRICAO_CRC  String?   @db.VarChar(15)
  UF_CRC         String?   @db.Char(2)
  FONE           String?   @db.VarChar(14)
  FAX            String?   @db.VarChar(14)
  LOGRADOURO     String?   @db.VarChar(100)
  NUMERO         String?   @db.VarChar(10)
  COMPLEMENTO    String?   @db.VarChar(100)
  BAIRRO         String?   @db.VarChar(60)
  CIDADE         String?   @db.VarChar(60)
  CEP            String?   @db.VarChar(8)
  MUNICIPIO_IBGE Int?      @db.UnsignedInt
  UF             String?   @db.Char(2)
  EMAIL          String?   @db.VarChar(250)
  pessoa         pessoa    @relation(fields: [ID_PESSOA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "contador_ibfk_1")
  empresa        empresa[]

  @@index([ID_PESSOA], map: "FK_PESSOA_CONTADOR")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model contato {
  ID               Int      @id @default(autoincrement()) @db.UnsignedInt
  ID_PESSOA        Int?     @db.UnsignedInt
  ID_EMPRESA       Int?     @db.UnsignedInt
  NOME             String?  @db.VarChar(100)
  EMAIL            String?  @db.VarChar(250)
  FONE_COMERCIAL   String?  @db.VarChar(14)
  FONE_RESIDENCIAL String?  @db.VarChar(14)
  FONE_CELULAR     String?  @db.VarChar(14)
  empresa          empresa? @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "contato_ibfk_1")
  pessoa           pessoa?  @relation(fields: [ID_PESSOA], references: [ID], onDelete: Cascade, map: "contato_ibfk_2")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_CONTATO")
  @@index([ID_PESSOA], map: "FK_PESSOA_CONTATO")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dfe {
  CNPJ_EMPRESA        String   @db.Char(14)
  CHAVE_ACESSO        String   @db.VarChar(44)
  NSU                 Int?
  OPERACAO            String?  @db.Char(1)
  SITUACAO            String?  @db.Char(1)
  MANIFESTADO         String?  @db.Char(1)
  MODELO              Int
  SERIE               Int
  NUMERO              Int
  CNPJ_CPF            String?  @db.VarChar(14)
  IE_RG               String?  @db.VarChar(20)
  RAZAO_SOCIAL        String?  @db.VarChar(60)
  EMISSAO             DateTime @db.DateTime(0)
  VALOR               Decimal? @db.Decimal(12, 2)
  MANIF_DHREGEVENTO   String?  @db.VarChar(20)
  MANIF_NPROT         String?  @db.VarChar(15)
  MANIF_CSTAT         Int?
  MANIF_XMOTIVO       Bytes?   @db.Blob
  MANIF_XML           Bytes?   @db.Blob
  DOCXML              Bytes?
  XML                 String?  @db.Char(1)
  NFE_FOI_IMPORTADA   String?  @default("N") @db.Char(1)
  MANIF_TIPO_EVENTO   String?  @db.VarChar(150)
  MANIF_CONFIRMADO_OP String?  @default("N") @db.Char(1)

  @@id([CNPJ_EMPRESA, CHAVE_ACESSO])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model empresa {
  ID                            Int                      @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA                    Int?                     @db.UnsignedInt
  ID_SINDICATO_PATRONAL         Int?                     @db.UnsignedInt
  ID_FPAS                       Int?                     @db.UnsignedInt
  ID_CONTADOR                   Int?                     @db.UnsignedInt
  RAZAO_SOCIAL                  String?                  @db.VarChar(150)
  NOME_FANTASIA                 String?                  @db.VarChar(150)
  CNPJ                          String?                  @db.VarChar(14)
  INSCRICAO_ESTADUAL            String?                  @db.VarChar(30)
  INSCRICAO_ESTADUAL_ST         String?                  @db.VarChar(30)
  INSCRICAO_MUNICIPAL           String?                  @db.VarChar(30)
  INSCRICAO_JUNTA_COMERCIAL     String?                  @db.VarChar(30)
  DATA_INSC_JUNTA_COMERCIAL     DateTime?                @db.Date
  TIPO                          String?                  @db.Char(1)
  DATA_CADASTRO                 DateTime?                @db.Date
  DATA_INICIO_ATIVIDADES        DateTime?                @db.Date
  SUFRAMA                       String?                  @db.VarChar(9)
  EMAIL                         String?                  @db.VarChar(250)
  IMAGEM_LOGOTIPO               String?                  @db.VarChar(250)
  CRT                           String?                  @db.Char(1)
  TIPO_REGIME                   String?                  @db.Char(1)
  ALIQUOTA_PIS                  Decimal?                 @db.Decimal(18, 6)
  CONTATO                       String?                  @db.VarChar(50)
  ALIQUOTA_COFINS               Decimal?                 @db.Decimal(18, 6)
  CODIGO_IBGE_CIDADE            Int?                     @db.UnsignedInt
  CODIGO_IBGE_UF                Int?                     @db.UnsignedInt
  CODIGO_TERCEIROS              Int?                     @db.UnsignedInt
  CODIGO_GPS                    Int?                     @db.UnsignedInt
  ALIQUOTA_SAT                  Decimal?                 @db.Decimal(18, 6)
  CEI                           String?                  @db.VarChar(12)
  CODIGO_CNAE_PRINCIPAL         String?                  @db.VarChar(7)
  SEGMENTO                      Int?                     @default(0)
  MEI                           String?                  @default("N") @db.Char(1)
  SITE                          String?                  @db.VarChar(100)
  TELEFONE                      String?                  @db.VarChar(60)
  ALIQUOTA_CREDITO_ICMS_SN      Decimal?                 @default(0.000000) @db.Decimal(18, 6)
  CAMINHO_ARQUIVO_BACKUP_BASE   String?                  @default("C:\\Bussolla\\Backup") @db.VarChar(1000)
  COMPACTAR_BACKUP              String?                  @default("N") @db.Char(1)
  DIAS_PARA_MANTER_BACKUP       String?                  @default("20") @db.Char(2)
  EXCLUIR_BACKUP_ORIGINAL       String?                  @default("N") @db.Char(1)
  AJUSTAR_IMAGEM_TELA_CAIXA     String?                  @default("N") @db.Char(1)
  BACKUP_TABELAS_MOVIMENTO      String?                  @default("S") @db.Char(1)
  CAMINHO_COPIA_ARQ_BACKUP_BASE String?                  @db.VarChar(1000)
  EXIBIR_TELA_DURANTE_BACKUP    String?                  @default("S") @db.Char(1)
  ULTIMO_NSU                    Int?
  DTHR_ULTIMA_CONSULTA          DateTime?                @db.DateTime(0)
  NUMERO_TENTATIVAS             String?                  @default("0") @db.Char(1)
  adm_parametro                 adm_parametro[]
  cargo                         cargo[]
  contato                       contato[]
  contador                      contador?                @relation(fields: [ID_CONTADOR], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_ibfk_1")
  fpas                          fpas?                    @relation(fields: [ID_FPAS], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_ibfk_2")
  sindicato                     sindicato?               @relation(fields: [ID_SINDICATO_PATRONAL], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_ibfk_3")
  empresa                       empresa?                 @relation("empresaToempresa", fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_ibfk_4")
  other_empresa                 empresa[]                @relation("empresaToempresa")
  empresa_cnae                  empresa_cnae[]
  empresa_pessoa                empresa_pessoa[]
  endereco                      endereco[]
  nfe_configuracao              nfe_configuracao[]
  nfe_numero                    nfe_numero[]
  setor                         setor[]
  tribut_operacao_fiscal        tribut_operacao_fiscal[]

  @@index([ID_EMPRESA], map: "FK_AUTO_REL_EMPRESA")
  @@index([ID_CONTADOR], map: "FK_CONTADOR_EMPRESA")
  @@index([ID_FPAS], map: "FK_FPAS_EMPRESA")
  @@index([ID_SINDICATO_PATRONAL], map: "FK_SINDICATO_EMPRESA")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model empresa_cnae {
  ID             Int     @id @default(autoincrement()) @db.UnsignedInt
  ID_CNAE        Int     @db.UnsignedInt
  ID_EMPRESA     Int     @db.UnsignedInt
  PRINCIPAL      String? @db.Char(1)
  RAMO_ATIVIDADE String? @db.VarChar(50)
  OBJETO_SOCIAL  String? @db.Text
  empresa        empresa @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_cnae_ibfk_1")
  cnae           cnae    @relation(fields: [ID_CNAE], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_cnae_ibfk_2")

  @@index([ID_CNAE], map: "FK_CNAE_EMPRESA")
  @@index([ID_EMPRESA], map: "FK_EMPRESA_CNAE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model empresa_pessoa {
  ID                Int     @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA        Int     @db.UnsignedInt
  ID_PESSOA         Int     @db.UnsignedInt
  RESPONSAVEL_LEGAL String? @db.Char(1)
  empresa           empresa @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_pessoa_ibfk_1")
  pessoa            pessoa  @relation(fields: [ID_PESSOA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "empresa_pessoa_ibfk_2")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_PESSOA")
  @@index([ID_PESSOA], map: "FK_PESSOA_EMPRESA")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model endereco {
  ID              Int      @id @default(autoincrement())
  ID_EMPRESA      Int?     @db.UnsignedInt
  ID_PESSOA       Int?     @db.UnsignedInt
  LOGRADOURO      String?  @db.VarChar(60)
  NUMERO          String?  @db.VarChar(10)
  COMPLEMENTO     String?  @db.VarChar(60)
  BAIRRO          String?  @db.VarChar(60)
  CIDADE          String?  @db.VarChar(60)
  CEP             String?  @db.VarChar(8)
  MUNICIPIO_IBGE  Int?     @db.UnsignedInt
  UF              String?  @db.Char(2)
  FONE            String?  @db.VarChar(14)
  FAX             String?  @db.VarChar(14)
  PRINCIPAL       String?  @db.Char(1)
  ENTREGA         String?  @db.Char(1)
  COBRANCA        String?  @db.Char(1)
  CORRESPONDENCIA String?  @db.Char(1)
  pessoa          pessoa?  @relation(fields: [ID_PESSOA], references: [ID], onDelete: Cascade, map: "endereco_ibfk_1")
  empresa         empresa? @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "endereco_ibfk_2")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_ENDERECO")
  @@index([ID_PESSOA], map: "FK_PESSOA_ENDERECO")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model estado_civil {
  ID            Int             @id @default(autoincrement()) @db.UnsignedInt
  NOME          String?         @db.VarChar(50)
  DESCRICAO     String?         @db.Text
  pessoa_fisica pessoa_fisica[]
}

model fpas {
  ID                       Int       @id @default(autoincrement()) @db.UnsignedInt
  CODIGO                   Int?      @db.UnsignedInt
  CNAE                     String?   @db.VarChar(14)
  ALIQUOTA_SAT             Decimal?  @db.Decimal(10, 0)
  DESCRICAO                String?   @db.VarChar(250)
  PERCENTUAL_INSS_PATRONAL Decimal?  @db.Decimal(18, 6)
  CODIGO_TERCEIRO          Decimal?  @db.Decimal(4, 0)
  PERCENTUAL_TERCEIROS     Decimal?  @db.Decimal(18, 6)
  empresa                  empresa[]
}

model intuiterp {
  ID          Int    @id @default(autoincrement()) @db.UnsignedInt
  SECAO       String @db.VarChar(80)
  PROPRIEDADE String @db.VarChar(80)
  VALOR       String @db.VarChar(600)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model municipio {
  ID                     Int     @id @default(autoincrement()) @db.UnsignedInt
  ID_UF                  Int     @db.UnsignedInt
  NOME                   String? @db.VarChar(100)
  CODIGO_IBGE            Int?    @db.UnsignedInt
  CODIGO_RECEITA_FEDERAL Int?    @db.UnsignedInt
  CODIGO_ESTADUAL        Int?    @db.UnsignedInt
  uf                     uf      @relation(fields: [ID_UF], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "municipio_ibfk_1")

  @@index([ID_UF], map: "FK_UF_MUNICIPIO")
}

model nfc_numero {
  ID         Int     @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA Int     @db.UnsignedInt
  SERIE      String? @db.Char(3)
  NUMERO     Int?    @default(0) @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model nfe_configuracao {
  ID                           Int     @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA                   Int     @db.UnsignedInt
  CERTIFICADO_DIGITAL_SERIE    String? @db.VarChar(100)
  CERTIFICADO_DIGITAL_CAMINHO  String? @db.VarChar(250)
  CERTIFICADO_DIGITAL_SENHA    String? @db.VarChar(100)
  TIPO_EMISSAO                 String? @db.Char(1)
  FORMATO_IMPRESSAO_DANFE      String? @db.Char(1)
  PROCESSO_EMISSAO             String? @db.Char(1)
  VERSAO_PROCESSO_EMISSAO      String? @db.VarChar(20)
  CAMINHO_LOGOMARCA            String? @db.VarChar(250)
  SALVAR_XML                   String? @db.Char(1)
  CAMINHO_SALVAR_XML           String? @db.VarChar(250)
  CAMINHO_SCHEMAS              String? @db.VarChar(250)
  CAMINHO_ARQUIVO_DANFE        String? @db.VarChar(250)
  CAMINHO_SALVAR_PDF           String? @db.VarChar(250)
  WEBSERVICE_UF                String? @db.Char(2)
  WEBSERVICE_AMBIENTE          String? @db.Char(1)
  WEBSERVICE_PROXY_HOST        String? @db.VarChar(100)
  WEBSERVICE_PROXY_PORTA       Int?    @db.UnsignedInt
  WEBSERVICE_PROXY_USUARIO     String? @db.VarChar(100)
  WEBSERVICE_PROXY_SENHA       String? @db.VarChar(100)
  WEBSERVICE_VISUALIZAR        String? @db.Char(1)
  EMAIL_SERVIDOR_SMTP          String? @db.VarChar(100)
  EMAIL_PORTA                  Int?    @db.UnsignedInt
  EMAIL_USUARIO                String? @db.VarChar(100)
  EMAIL_SENHA                  String? @db.VarChar(100)
  EMAIL_ASSUNTO                String? @db.VarChar(100)
  EMAIL_AUTENTICA_SSL          String? @db.Char(1)
  EMAIL_TEXTO                  String? @db.Text
  TIPO_SERVICO                 String? @default("N") @db.VarChar(1)
  TIPO_COMBUSTIVEL             String? @default("N") @db.VarChar(1)
  TIPO_VEICULO                 String? @default("N") @db.VarChar(1)
  TIPO_MEDICAMENTO             String? @default("N") @db.VarChar(1)
  TIPO_ARMAMENTO               String? @default("N") @db.VarChar(1)
  TIPO_IMPOSTO_IMPORTACAO      String? @default("N") @db.VarChar(1)
  ID_OPERACAO_FISCAL_PADRAO    Int?
  CONSUMIDOR_FINAL             String? @default("S") @db.Char(1)
  SELECIONAR_IMPRESSORA        String? @default("N") @db.Char(1)
  IDCSC                        String? @db.VarChar(6)
  CSC                          String? @db.VarChar(50)
  SOMAR_PRODUTOS_IGUAIS        String? @default("N") @db.Char(1)
  ID_OPERACAO_FISCAL_DEVOLUCAO Int?
  ULTIMO_NR_NFE                Int?
  ULTIMO_NR_NFCE               Int?
  VERSAO_LAYOUT_DF             String? @default("ve310") @db.VarChar(10)
  WEBSERVICE_TIMEOUT           Int?    @default(5)
  UTILIZAR_OPEN_SSL            String? @default("N") @db.Char(1)
  CAMINHO_LAYOUT_NFCE          String? @default("C:\\Bussolla\\NFe\\Schemas\\DANFeNFCe[Reduzida].fr3") @db.VarChar(200)
  CAMINHO_CERTIFICADO_A1       String? @db.VarChar(200)
  CONTINGENCIA_DUAS_VIAS       String? @default("N") @db.Char(1)
  CONTINGENCIA_AUTOMATICO      String? @default("S") @db.Char(1)
  NSU                          Int?
  empresa                      empresa @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "nfe_configuracao_ibfk_1")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_NFE_CONFIG")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model nfe_numero {
  ID         Int     @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA Int     @db.UnsignedInt
  SERIE      String? @db.Char(3)
  NUMERO     Int?    @default(0) @db.UnsignedInt
  empresa    empresa @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "nfe_numero_ibfk_1")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_NFE_NUMERO")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model nivel_formacao {
  ID                   Int           @id @default(autoincrement()) @db.UnsignedInt
  NOME                 String?       @db.VarChar(100)
  DESCRICAO            String?       @db.Text
  GRAU_INSTRUCAO_CAGED Int?          @db.UnsignedInt
  GRAU_INSTRUCAO_SEFIP Int?          @db.UnsignedInt
  GRAU_INSTRUCAO_RAIS  Int?          @db.UnsignedInt
  colaborador          colaborador[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pais {
  ID        Int     @id @default(autoincrement()) @db.UnsignedInt
  CODIGO    Int?    @db.UnsignedInt
  NOME_EN   String? @db.VarChar(100)
  NOME_PTBR String? @db.VarChar(100)
  SIGLA2    String? @db.Char(2)
  SIGLA3    String? @db.Char(3)
  uf        uf[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model papel {
  ID              Int       @id @default(autoincrement()) @db.UnsignedInt
  NOME            String?   @db.VarChar(20)
  DESCRICAO       String?   @db.Text
  ACESSO_COMPLETO String?   @db.Char(1)
  usuario         usuario[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pessoa {
  ID              Int               @id @default(autoincrement()) @db.UnsignedInt
  NOME            String            @db.VarChar(150)
  TIPO            String            @db.Char(1)
  EMAIL           String?           @db.VarChar(250)
  SITE            String?           @db.VarChar(250)
  CLIENTE         String?           @db.Char(1)
  FORNECEDOR      String?           @db.Char(1)
  COLABORADOR     String?           @db.Char(1)
  CONVENIO        String?           @db.Char(1)
  CONTADOR        String?           @db.Char(1)
  TRANSPORTADORA  String?           @db.Char(1)
  ID_PESSOA_PDV   Int?              @unique(map: "ID_PESSOA_PDV") @db.UnsignedInt
  EXPORTAR_PDV    String            @default("N") @db.Char(1)
  ID_EMPRESA      Int?              @db.UnsignedInt
  colaborador     colaborador[]
  contador        contador[]
  contato         contato[]
  empresa_pessoa  empresa_pessoa[]
  endereco        endereco[]
  pessoa_fisica   pessoa_fisica[]
  pessoa_juridica pessoa_juridica[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pessoa_empresa {
  ID            Int  @id @unique(map: "ID") @default(autoincrement()) @db.UnsignedInt
  ID_PESSOA     Int  @db.UnsignedInt
  ID_PESSOA_PDV Int? @db.UnsignedInt
  ID_EMPRESA    Int? @db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pessoa_fisica {
  ID                      Int           @id @default(autoincrement()) @db.UnsignedInt
  ID_PESSOA               Int           @db.UnsignedInt
  ID_ESTADO_CIVIL         Int?          @db.UnsignedInt
  CPF                     String?       @db.VarChar(11)
  RG                      String?       @db.VarChar(20)
  ORGAO_RG                String?       @db.VarChar(20)
  DATA_EMISSAO_RG         DateTime?     @db.Date
  DATA_NASCIMENTO         DateTime?     @db.Date
  SEXO                    String?       @db.Char(1)
  NATURALIDADE            String?       @db.VarChar(100)
  NACIONALIDADE           String?       @db.VarChar(100)
  RACA                    String?       @db.Char(1)
  TIPO_SANGUE             String?       @db.Char(3)
  CNH_NUMERO              String?       @db.VarChar(20)
  CNH_CATEGORIA           String?       @db.Char(2)
  CNH_VENCIMENTO          DateTime?     @db.Date
  TITULO_ELEITORAL_NUMERO String?       @db.VarChar(20)
  TITULO_ELEITORAL_ZONA   Int?          @db.UnsignedInt
  TITULO_ELEITORAL_SECAO  Int?          @db.UnsignedInt
  RESERVISTA_NUMERO       String?       @db.VarChar(20)
  RESERVISTA_CATEGORIA    Int?          @db.UnsignedInt
  NOME_MAE                String?       @db.VarChar(100)
  NOME_PAI                String?       @db.VarChar(100)
  APELIDO                 String?       @db.VarChar(100)
  estado_civil            estado_civil? @relation(fields: [ID_ESTADO_CIVIL], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "pessoa_fisica_ibfk_1")
  pessoa                  pessoa        @relation(fields: [ID_PESSOA], references: [ID], onDelete: Cascade, map: "pessoa_fisica_ibfk_2")

  @@index([ID_ESTADO_CIVIL], map: "FK_ESTADO_CIVIL_PF")
  @@index([ID_PESSOA], map: "FK_PESSOA_FISICA")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pessoa_juridica {
  ID                  Int       @id @default(autoincrement()) @db.UnsignedInt
  ID_PESSOA           Int       @db.UnsignedInt
  CNPJ                String?   @db.VarChar(14)
  FANTASIA            String?   @db.VarChar(150)
  INSCRICAO_MUNICIPAL String?   @db.VarChar(30)
  INSCRICAO_ESTADUAL  String?   @db.VarChar(30)
  DATA_CONSTITUICAO   DateTime? @db.Date
  TIPO_REGIME         String?   @db.Char(1)
  CRT                 String?   @db.Char(1)
  SUFRAMA             String?   @db.VarChar(9)
  pessoa              pessoa    @relation(fields: [ID_PESSOA], references: [ID], onDelete: Cascade, map: "pessoa_juridica_ibfk_1")

  @@index([ID_PESSOA], map: "FK_PESSOA_JURIDICA")
}

model plano_conta {
  ID             Int              @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA     Int              @db.UnsignedInt
  NOME           String?          @db.VarChar(100)
  DATA_INCLUSAO  DateTime?        @db.Date
  MASCARA        String?          @db.VarChar(50)
  NIVEIS         Int?             @db.UnsignedInt
  contabil_conta contabil_conta[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model plano_conta_ref_sped {
  ID              Int              @id @default(autoincrement()) @db.UnsignedInt
  COD_CTA_REF     String?          @db.VarChar(30)
  DESCRICAO       String?          @db.Text
  ORIENTACOES     String?          @db.Text
  INICIO_VALIDADE DateTime?        @db.Date
  FIM_VALIDADE    DateTime?        @db.Date
  TIPO            String?          @db.Char(1)
  contabil_conta  contabil_conta[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model setor {
  ID          Int           @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA  Int           @db.UnsignedInt
  NOME        String?       @db.VarChar(50)
  DESCRICAO   String?       @db.Text
  colaborador colaborador[]
  empresa     empresa       @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "setor_ibfk_1")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_SETOR")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sindicato {
  ID                Int             @id @default(autoincrement()) @db.UnsignedInt
  ID_CONTABIL_CONTA Int?            @db.UnsignedInt
  NOME              String?         @db.VarChar(100)
  CODIGO_BANCO      Int?            @db.UnsignedInt
  CODIGO_AGENCIA    Int?            @db.UnsignedInt
  CONTA_BANCO       String?         @db.VarChar(20)
  CODIGO_CEDENTE    String?         @db.VarChar(30)
  LOGRADOURO        String?         @db.VarChar(100)
  NUMERO            String?         @db.VarChar(10)
  BAIRRO            String?         @db.VarChar(100)
  MUNICIPIO_IBGE    Int?            @db.UnsignedInt
  UF                String?         @db.Char(2)
  FONE1             String?         @db.VarChar(14)
  FONE2             String?         @db.VarChar(14)
  EMAIL             String?         @db.VarChar(100)
  TIPO_SINDICATO    String?         @db.Char(1)
  DATA_BASE         DateTime?       @db.Date
  PISO_SALARIAL     Decimal?        @db.Decimal(18, 6)
  CNPJ              String?         @db.VarChar(14)
  colaborador       colaborador[]
  empresa           empresa[]
  contabil_conta    contabil_conta? @relation(fields: [ID_CONTABIL_CONTA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "sindicato_ibfk_1")

  @@index([ID_CONTABIL_CONTA], map: "FK_CTA_CONTAB_SINDICATO")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model situacao_colaborador {
  ID          Int           @id @default(autoincrement()) @db.UnsignedInt
  CODIGO      String?       @db.Char(3)
  NOME        String?       @db.VarChar(100)
  DESCRICAO   String?       @db.Text
  colaborador colaborador[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tipo_admissao {
  ID          Int           @id @default(autoincrement()) @db.UnsignedInt
  CODIGO      String?       @db.Char(3)
  NOME        String?       @db.VarChar(100)
  DESCRICAO   String?       @db.Text
  colaborador colaborador[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tipo_colaborador {
  ID          Int           @id @default(autoincrement()) @db.UnsignedInt
  NOME        String?       @db.VarChar(20)
  DESCRICAO   String?       @db.Text
  colaborador colaborador[]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tribut_operacao_fiscal {
  ID                 Int     @id @default(autoincrement()) @db.UnsignedInt
  ID_EMPRESA         Int     @db.UnsignedInt
  DESCRICAO          String? @db.VarChar(100)
  DESCRICAO_NA_NF    String? @db.VarChar(100)
  CFOP               Int?    @db.UnsignedInt
  OBSERVACAO         String? @db.Text
  FINALIDADE_EMISSAO String? @db.Char(1)
  CONSUMIDOR_FINAL   String? @db.Char(1)
  TIPO_OPERACAO      String? @db.Char(1)
  TIPO_PRECO_PRODUTO String? @default("V") @db.Char(1)
  empresa            empresa @relation(fields: [ID_EMPRESA], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "tribut_operacao_fiscal_ibfk_1")

  @@index([ID_EMPRESA], map: "FK_EMPRESA_OPERACAO_FISCAL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model uf {
  ID          Int         @id @default(autoincrement()) @db.UnsignedInt
  ID_PAIS     Int         @db.UnsignedInt
  SIGLA       String?     @db.Char(2)
  NOME        String?     @db.VarChar(50)
  CODIGO_IBGE Int?        @db.UnsignedInt
  municipio   municipio[]
  pais        pais        @relation(fields: [ID_PAIS], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "uf_ibfk_1")

  @@index([ID_PAIS], map: "FK_PAIS_UF")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model usuario {
  ID                                      Int         @id @default(autoincrement()) @db.UnsignedInt
  ID_COLABORADOR                          Int         @db.UnsignedInt
  ID_PAPEL                                Int         @db.UnsignedInt
  LOGIN                                   String?     @db.VarChar(20)
  SENHA                                   String?     @db.VarChar(40)
  DATA_CADASTRO                           DateTime?   @db.Date
  ADMINISTRADOR                           String?     @db.Char(1)
  ID_CONTA_CAIXA                          Int?        @db.UnsignedInt
  PERMISSAO_LIBERAR_CREDIARIO             String?     @default("N") @db.Char(1)
  PERMISSAO_ALTERAR_LANCAMENTO            String?     @default("N") @db.Char(1)
  PERMISSAO_SENHA                         String?     @db.VarChar(20)
  PERMISSAO_TODOS_CAIXAS                  String?     @default("N") @db.Char(1)
  PERMISSAO_TRANSFERENCIA                 String?     @default("N") @db.Char(1)
  PERMISSAO_LANCAMENTO                    String?     @default("N") @db.Char(1)
  PERMISSAO_ESTOQUE                       String?     @default("N") @db.Char(1)
  ALIQUOTA_DESCONTO_MAXIMO                Int?        @default(0)
  PERMISSAO_ALTERAR_VALOR                 String?     @default("N") @db.Char(1)
  PERMISSAO_CANCELAR                      String?     @default("N") @db.Char(1)
  PERMISSAO_CAIXA_FAZER_ORCAMENTO         String?     @default("N") @db.Char(1)
  TIPOALTERACAOESTOQUE                    String?     @db.Char(1)
  PERMISSAO_LIBERAR_ESTOQUE_NEGATIVO      String?     @default("N") @db.Char(20)
  PERMISSAO_DEVOLUCAO                     String?     @default("N") @db.Char(1)
  PERMISSAO_LANCAMENTO_RETRO              String?     @default("N") @db.Char(1)
  PERMISSAO_RENEGOCIACAO                  String?     @default("N") @db.Char(1)
  PERMISSAO_EXCLUIR_ITEM_VENDA            String      @default("N") @db.Char(1)
  PERMISSAO_ZERAR_ESTOQUE                 String?     @default("N") @db.Char(1)
  PERMISSAO_ADD_VALE                      String?     @default("N") @db.Char(1)
  PERMISSAO_ALTERAR_ESTOQUE_ENTRADA       String?     @default("N") @db.Char(1)
  PERMISSAO_ALTERAR_ESTOQUE_SAIDA         String?     @default("N") @db.Char(1)
  PERMISSAO_ALTERAR_ESTOQUE_AJUSTE        String?     @default("N") @db.Char(1)
  PERMISSAO_ALTERAR_ESTOQUE_TRANSF        String?     @default("N") @db.Char(1)
  PERMISSAO_VENCIMENTO_RETROATIVO         String?     @default("N") @db.Char(1)
  PERMISSAO_LIMPAR_VENDA                  String?     @default("N") @db.Char(1)
  PERMISSAO_ABRIR_GAVETA                  String?     @default("N") @db.Char(1)
  PERMISSAO_RELATORIO_CAIXA               String?     @default("N") @db.Char(1)
  PERMISSAO_ADD_VALE_RECEBIMENTO_SUPERIOR String?     @default("N") @db.Char(1)
  PERMISSAO_ACESSAR_BUVENDA               String?     @default("S") @db.Char(1)
  PERMISSAO_BAIXAR_TITULO                 String?     @default("N") @db.Char(1)
  PERMISSAO_VENDA_CUSTO                   String?     @default("N") @db.Char(1)
  PERMISSAO_ALTERAR_ITEM_LISTA            String?     @default("S") @db.Char(1)
  PERMISSAO_EDITAR_PRODUTO_PDV            String?     @default("N") @db.Char(1)
  PERMISSAO_ALTERAR_CONTA_RECEBER         String?     @default("S") @db.Char(20)
  PERMISSAO_ALTERAR_CONTA_PAGAR           String?     @default("S") @db.Char(1)
  PERMISSAO_CANCELAR_PARCELA_RECEBER      String?     @default("N") @db.Char(1)
  PERMISSAO_CANCELAR_PARCELA_PAGAR        String?     @default("N") @db.Char(1)
  ACESSO_LANCAMENTO_RECEBER               String?     @default("S") @db.Char(1)
  ACESSO_LANCAMENTO_PAGAR                 String?     @default("S") @db.Char(1)
  PERMISSAO_EXCLUIR_PAGAMENTO             String?     @default("N") @db.Char(1)
  PERMISSAO_EXCLUIR_RECEBIMENTO           String?     @default("N") @db.Char(1)
  PERMISSAO_INCLUIR_PRODUTO_PDV           String?     @default("S") @db.Char(1)
  PERMISSAO_ALTERAR_PRODUTO_PDV           String?     @default("N") @db.Char(1)
  PERMISSAO_DASHBOARD                     String?     @default("S") @db.VarChar(20)
  ABA_MENU_GESTOR                         Int?        @default(5)
  PERMISSAO_ACESSAR_CONSULTAR_VENDAS      String?     @default("S") @db.Char(1)
  USA_LEITOR_CODIGO_BARRAS_AUTORIZAR      String?     @default("N") @db.Char(1)
  PERMISSAO_PDV_VENDER_DIVERSOS           String?     @default("S") @db.Char(1)
  PERMISSAO_PDV_ACESSAR_FUNCOES           String?     @default("S") @db.Char(1)
  PERMISSAO_FECHA_CAIXA_PED_ABERTO        String?     @default("N") @db.Char(1)
  auditoria                               auditoria[]
  colaborador                             colaborador @relation(fields: [ID_COLABORADOR], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "usuario_ibfk_1")
  papel                                   papel       @relation(fields: [ID_PAPEL], references: [ID], onDelete: NoAction, onUpdate: NoAction, map: "usuario_ibfk_2")

  @@index([ID_COLABORADOR], map: "FK_COLABORADOR_USUARIO")
  @@index([ID_PAPEL], map: "FK_PAPEL_USUARIO")
}
